# Inspect scripts

This folder contains optional PowerShell inspection helpers for run roots and generated artifacts.

These scripts are not part of the standard verification gate. Use `scripts/runbook/README.md` for the canonical runbook path.